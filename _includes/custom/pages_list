<li>{{ "a~b" | split:'~' }}|{{ (page.url | split:'/' | first | prettyprint) }}</li>
{% for weight in (1..10) %}
  {% for node in pages_list %}
    {% if node.weight == weight %}
      {% if node.title != null %}
        {% if group == null or group == node.group %}
          {% if page.url == node.url %}
          <li class="active"><a href="{{ BASE_PATH }}{{node.url}}" class="active">{{node.title}}|{{ (node.url | split:'/' | prettyprint) }}</a></li>
          {% else %}
          <li><a href="{{ BASE_PATH }}{{node.url}}">{{node.title}}|{{ (node.url | split:'/' | prettyprint) }}</a></li>
          {% endif %}
        {% endif %}
      {% endif %}
    {% endif %}
  {% endfor %}
{% endfor %}
{% assign pages_list = nil %}
{% assign group = nil %}